---

import {  } from "../../js/utils";

export async function getStaticPaths() {
  return [
    { 
      params: { category: "R" },
      props: {},
    },
  ];
}

const { tag } = Astro.params;
const { tag } = Astro.props;
---