---

import {  } from "../../js/utils";

export async function getStaticPaths() {
  return [
    { 
      params: { category: "Reference Do" },
      props: {},
    },
  ];
}

const { tag } = Astro.params;
const { tag } = Astro.props;
---