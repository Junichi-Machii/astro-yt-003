---

import {  } from "../../js/utils";

export async function getStaticPaths() {
  return [
    { 
      params: { category: "Referec" },
      props: {},
    },
  ];
}

const { tag } = Astro.params;
const { tag } = Astro.props;
---