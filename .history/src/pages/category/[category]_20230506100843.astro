---

import {  } from "../../js/utils";

export async function getStaticPaths() {
  return [
    { 
      params: { category: "Referen" },
      props: {},
    },
  ];
}

const { tag } = Astro.params;
const { tag } = Astro.props;
---